<h2>ブックマークした投稿</h2>

<% @posts.each do |post| %>
  <div class="card mb-3">
    <div class="card-body">
      <h5 class="card-title"><%= link_to post.location, post_path(post) %></h5>
      <p class="card-text"><%= truncate(post.text, length: 100) %></p>
    </div>
  </div>
<% end %>